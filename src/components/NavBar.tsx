import React from 'react';
import './components.sass'
const NavBar = () => {
    return (
        <nav className={'navbar'}>
            <h2>Hello</h2>
        </nav>
    );
};

export default NavBar;